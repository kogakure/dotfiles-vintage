 #!/bin/sh

# Runs an accessibility audit against a URL
yarn add global a11y

# Babel command line.
yarn add global babel-cli

# Custom parser for ESLint
yarn add global babel-eslint

# A Babel preset for each environment.
npm install babel-preset-env

# BackstopJS: Catch CSS curveballs.
yarn add global backstopjs

# Browser package manager
yarn add global bower

# Live CSS Reload & Browser Syncing
yarn add global browser-sync

# caniuse for the command line
# ➜ caniuse border-radius
yarn add global caniuse-cmd

# Git commit, but play nice with conventions.
# ➜ git cz
yarn add global commitizen

# Canvas graphics API backed by Cairo
yarn add global canvas

# Prompts for conventional changelog standard.
yarn add global cz-conventional-changelog

# CSSO (CSS Optimizer) is a CSS minifier with structural optimisations
# ➜ csso in.css out.css
yarn add global csso

# runs Node.js programs through Chromium DevTools
# ➜ devtool src/app.js
yarn add global devtool

# An AST-based pattern checker for JavaScript.
yarn add global eslint
yarn add global eslint-plugin-babel
yarn add global eslint-plugin-chai
yarn add global eslint-plugin-chai-expect
yarn add global eslint-plugin-flowtype
yarn add global eslint-plugin-html
yarn add global eslint-plugin-jasmine
yarn add global eslint-plugin-jsdoc
yarn add global eslint-plugin-json
yarn add global eslint-plugin-jsx
yarn add global eslint-plugin-jsx-extras
yarn add global eslint-plugin-lodash
yarn add global eslint-plugin-mocha
yarn add global eslint-plugin-node
yarn add global eslint-plugin-react
yarn add global eslint-plugin-redux

# Express' application generator
# ➜ express myapp --hogan -c less
yarn add global express-generator

# Test your download speed using fast.com
# ➜ fast
yarn add global fast-cli

# Command-Line Interface for Firebase
yarn add global firebase-tools

# Library for streamlining application deployment or systems administration tasks
# ➜ fly
yarn add global flightplan

# Just the node wrapper for the Galen Framework.
# ➜ galen
yarn add global galenframework-cli

# React.js Static Site Generator
yarn add global gatsby

# Convert any video file to an optimized animated GIF
yarn add global gifify

# CLI to generate books and documentation using gitbook
yarn add global gitbook-cli

# Command line interface for gulp
yarn add global gulp-cli

# Hops
yarn add global hops-cli

# Small JSON file server for REST API mock
# ➜ touch db.json; json-server --watch db.json
yarn add global json-server

# Tool for managing JavaScript projects with multiple packages (BETA)
yarn add global lerna@^2.0.0-beta

# Lighthouse analyzes web apps and web pages, collecting modern performance metrics and insights on developer best practices.
yarn add global lighthouse

# simple, flexible, fun test framework
yarn add global mocha

# Wrap web apps natively
yarn add global nativefier

# The Netlify CLI tools lets you create, deploy, and delete new sites straight from your terminal.
yarn add global netlify-cli

# Tool to create icns files for OSX
yarn add global node-icns

# Simple monitor script for use during development of a node.js app.
yarn add global nodemon

# Check for outdated, incorrect, and unused dependencies.
# ➜ npm-check -g
yarn add global npm-check

# Find newer versions of dependencies than what your package.json or bower.json allows
# ➜ ncu
yarn add global npm-check-updates

# Executes <command> either from a local node_modules/.bin, or from a central cache, installing any packages needed in order for <command> to run.
yarn add global npx

# Capture website screenshots
# ➜ pageres [ todomvc.com 1200x1000 ] [ yeoman.io 800x600 1200x1000 ] --crop
yarn add global pageres-cli

# Single-Command Node.js Compiler
yarn add global pkg

# Start building a Preact Progressive Web App in seconds.
yarn add global preact-cli

# Prettier is an opinionated code formatter
yarn add global prettier
yarn add global prettier-stylelint

# Package for formatting JSON data in a coloured YAML-style, perfect for CLI output
# ➜ prettyjson package.json
# ➜ curl https://api.github.com/users/rafeca | prettyjson
yarn add global prettyjson

# CLI for postcss
yarn add global postcss-cli

# A commandline tool for Polymer projects
yarn add global polymer-cli

# Modern CSS linter
yarn add global stylelint
yarn add global stylelint-config-standard
yarn add global stylelint-config-prettier

# Analyze and debug space usage through source maps
yarn add global source-map-explorer

# Node.js module and command-line tool for exporting SVG to PNG and JPEG.
yarn add global svgexport

# Nodejs-based tool for optimizing SVG vector graphics files
yarn add global svgo

# CLI tool for downloading your precious Tumblr likes.
yarn add global tumblr-lks-downldr-cli

# JavaScript parser, mangler/compressor and beautifier toolkit
yarn add global uglify-js

# A simple CLI for scaffolding Vue.js projects.
yarn add global vue-cli

# Packs CommonJs/AMD modules for the browser. Allows to split your codebase into multiple bundles, which can be loaded on demand. Support loaders to preprocess files, i.e. json, jade, coffee, css, less, ... and your custom stuff.
yarn add global webpack

# Serves a webpack app. Updates the browser on changes.
yarn add global webpack-dev-server

# Fast, reliable, and secure dependency management.
yarn add global yarn
